name: Discord Forum Notification
on:
  push:
    branches: [ "main" ]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        run: |
          # 깃허브 금고에서 안전하게 주소와 ID를 가져와 결합합니다.
          TARGET_URL="${{ secrets.DISCORD_WEBHOOK_URL }}?thread_id=${{ secrets.DISCORD_THREAD_ID }}"
          
          # 보낼 메시지 내용을 깨끗하게 정리합니다.
          PAYLOAD='{"content": "🔨 **${{ github.repository }}** 저장소에 새 커밋이 푸시되었습니다!\n- 작성자: ${{ github.actor }}\n- 브랜치: ${{ github.ref_name }}\n- 메시지: ${{ github.event.head_commit.message }}"}'
          
          # 디스코드로 발송합니다.
          curl -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$TARGET_URL"
